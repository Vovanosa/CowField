# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent
AGENTS.md
mobile-migration-plan.md

.env

# Database dumps, but NOT the Prisma migration history — those are the schema's only record and the
# deploy path for a fresh database. `*.sql` alone silently kept every migration out of git.
*.sql
!prisma/migrations/**/*.sql
docs/logbook.md
docs/project-reference.md
docs/roadmap.md
